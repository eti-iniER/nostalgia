nostalgia.eti-ini.me {
    # Handle API requests - strip /api prefix when proxying
    handle /api/* {
        reverse_proxy django:8000
    }

    # Handle static files with fallback to index.html for SPA routing
    handle {
        root * /usr/share/caddy
        try_files {path} /index.html
        file_server
    }
}